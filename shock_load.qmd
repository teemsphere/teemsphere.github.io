---
execute:
  freeze: auto
---

# Model shocks {#shocks}

```{r, message = FALSE, warning = FALSE, echo = FALSE}
knitr::opts_knit$set(root.dir = fs::dir_create(tempfile()))
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", eval = TRUE)
options(crayon.enabled = FALSE)
```

The model shock is a percentage change quantity applied to an exogenous variable that will lead to an adjustment in endogenous model variables according to their relationships as specified in Equations within the Tablo file.

## Shock types

### Uniform shocks

### Custom shocks

### Scenario shocks

## Shock file