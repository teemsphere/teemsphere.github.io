---
execute:
  freeze: auto
---

# Model closure {#closures}

```{r, message = FALSE, warning = FALSE, echo = FALSE}
knitr::opts_knit$set(root.dir = fs::dir_create(tempfile()))
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", eval = TRUE)
options(crayon.enabled = FALSE)
```

The model closure consists of the list of exogenous variables that are available to be shocked. This list can be altered with "swaps".

## Closure files
User-provided
Standard internal

## Swaps
